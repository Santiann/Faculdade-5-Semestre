#include <stdio.h>
#include <stdlib.h>

/**
 * Explique o que é alocação dinâmica de memória. Descreva o conceito de alocação dinâmica
 * de memória e explique por que é útil em programação.
 * 
 * Reposta:
 * 
 * A alocação dinâmica de memória permite ao programador reservar uma quantidade específica de memória
 * em tempo de execução, ao invés de determinar o tamanho previamente, como ocorre na alocação estática.
 * Isso é útil quando o tamanho dos dados que será manipulado não é conhecido de antemão.
 *
 * Com a alocação dinâmica, é possível criar estruturas de dados flexíveis, como listas ligadas, árvores e filas,
 * que podem crescer ou diminuir conforme necessário. No entanto, é essencial liberar a memória após o uso para
 * evitar problemas como vazamentos de memória e queda de performance. O uso inadequado pode causar erros como
 * fragmentação ou sobrecarga de memória, prejudicando o desempenho do sistema.
 */  